/** Tracks to hide on the dashboard keeper card (collection-focused UI only). */
export const DASHBOARD_HIDDEN_BADGE_PROGRESS_KEYS = new Set(['sellerNext', 'reviewNext', 'sexPollNext'])

/** Earned badge keys to hide on the dashboard (marketplace / reviews / sex-ID). */
export const DASHBOARD_HIDDEN_BADGE_KEYS = new Set([
  'seller_listed_1',
  'seller_listed_5',
  'seller_listed_15',
  'seller_reviewed_1',
  'seller_reviewed_5',
  'sex_poll_10',
  'sex_poll_50',
])
