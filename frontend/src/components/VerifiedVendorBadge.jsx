import VerifiedOriginBadge from './VerifiedOriginBadge'

/** @deprecated Use VerifiedOriginBadge — kept for existing imports during Label Studio / Origin rollout. */
export default function VerifiedVendorBadge(props) {
  return <VerifiedOriginBadge {...props} />
}
